!
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to right, #3494e6, #ec6ead);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .login-container {
            background-color: rgba(255, 255, 255, 0.7); /* Adjust the alpha value for transparency */
            padding: 20px;
            border-radius: 50px;
            box-shadow: 5px 5px 10px;
            text-align: center;
            animation: fadeIn 0.8s ease-in-out;
            width: 80%; /* Adjust the width as needed */
            max-width: 400px; /* Set a maximum width */
        }

        h2 {
            color: #333;
        }

        .user-logo {
            width: 80px;
            height: 80px;
            background-color: #4CAF50;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px; /* Center the logo and provide some space below it */
            animation: spin 2s linear infinite; /* Add rotation animation */
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        label {
            display: block;
            margin-top: 10px;
            color: #555;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-top: 6px;
            margin-bottom: 12px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 10px;
            transition: background-color 0.3s, border-color 0.3s;
        }

        input:hover {
            background-color: #f0f0f0; /* Change the background color on hover */
            border-color: violet; /* Change the border color on hover */
        }

        input:focus {
            outline: none;
            border-color: #4CAF50; /* Change the border color when focused */
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #45a049;
        }

        .forgot-password {
            margin-top: 10px;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Media queries for responsiveness */
        @media (max-width: 600px) {
            .login-container {
                width: 90%;
            }
        }
    </style>
    <title>Login</title>
</head>
<body>
    {{>navbar}}
    <div class="login-container">
        <div class="user-logo">👤</div>
        <form class="from-signin"  action="/login" method="POST">
            <h2>Login</h2>
            <label for="AdminLoginId">Admin ID:</label>
            <input type="email" id="AdminName" name="email" required>

            <label for="password">Password:</label>
            <input type="password" id="AdminPassword" name="password" required>

            <button type="submit" name="Signin">Login</button>

            <div class="forgot-password">
                <a href="#" id="forgotPasswordLink">Forgot Password?</a>
            </div>
         </form>
    </div>

    </body>
</html>
